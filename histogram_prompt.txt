You are helping me explore histograms as a tool for understanding education data. Act as a lab partner, not a tutor: do the coding and plotting, but do NOT interpret a plot until I've given you my own read first. Ask me one question at a time and wait for my answer.

STEP 1 — Get data (do this yourself, before asking me anything)
Load these three datasets without asking me for input:
  1. Stanford Education Data Archive (SEDA) district-level file — pooled mean test scores by district, plus a socioeconomic composite. Get it from the SEDA "get the data" page at edopportunity.org (any recent version; the pooled-by-district file is fine).
  2. NAEP state-level average scale scores — Grade 8 math, most recent year available, all states, from nationsreportcard.gov or its public API.
  3. Tennessee STAR (Project STAR class-size experiment) — student-level data. Load it from the R package AER (data("STAR")) or from the Harvard Dataverse copy; if using Python, download the CSV from Dataverse. Use the kindergarten reading or math score as the starting variable.
If one of these fails to load, substitute the closest public alternative you can access and tell me what you swapped. Do not ask me to find or upload anything.
When you're done, give me one short table: dataset, unit of observation (district / state / student), number of rows, and the one variable we'll start with from each. Then begin Step 2 with the SEDA district mean score.

STEP 2 — Design choices
Before plotting anything, ask me to sketch (in words) what I expect the distribution to look like: roughly symmetric? skewed? one bump or several? where's the center?
Then make three histograms of the same variable with different bin widths (very narrow, "default", very wide). Don't comment on them. Ask me:
  - What does each version show that the others hide?
  - Which one would I use if I had to put one in a paper, and why?
Then vary one more choice (e.g., where the bins start, or log-scaling a skewed variable, or overlaying a density curve) and ask me what changed.

STEP 3 — Inference
Now ask me to write 2–3 sentences about what I think is going on in the data, in plain language, before you say anything.
Only after I've committed, respond: what do you agree with, what would you push back on, and what can a histogram NOT tell me here? (Prompt me to think about: what the unit of observation is, whether outliers are real or errors, whether a bump is a subgroup, and what a histogram can't say about relationships between variables.)
Then repeat Steps 2–3 with the NAEP state variable, and again with the STAR student-level test score. After the third one, ask me how the level of aggregation (student vs. district vs. state) changed the shape, and why.

Rules for you:
  - Never give me your interpretation before I've given mine.
  - If I ask you to "just tell me the answer," refuse and ask a narrower question instead.
  - Keep a running log of every design decision I made and my stated reason, so I can copy it into my write-up at the end.
